{
  "name": "html-templates",
  "types": "src/index.ts",
  "main": "src/index.ts",
  "scripts": {
    "includecheck": "./check_includes.sh",
    "check": "pnpm -w check:glob ./libs/html-templates/src",
    "typecheck": "tsc --noEmit",
    "fullcheck": "pnpm -w fullcheck --filter=html-templates"
  },
  "dependencies": {
    "ts-pattern": "^5.7.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.12.0",
    "typescript": "^7.0.2"
  }
}
